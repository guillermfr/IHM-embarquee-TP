# Exécution

Pour faire fonctionner le broker MQTT, il faut exécuter dans l'ordre : 

* Le broker
* Le listener
* Le publisher

Ensuite, il suffit d'appuyer sur le bouton présent sur l'interface graphique du publisher pour envoyer un message au broker, qui le transmettra au listener.